<%= link_to transition_path(notification) do %>
  <div>
    <%= link_to notification.subject.user.user_name, user_path(notification.subject.user) %>
    があなたの
    <%= link_to 'グループチャット', group_room_path(notification.subject.group_room) %>
    にメッセージを送信しました
    <%= " (#{time_ago_in_words(notification.created_at)}前)" %>
  </div>
<% end %>