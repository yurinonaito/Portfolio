<% if obj.errors.any? %>
  <div id="error_explanation">
    <h3><%= pluralize(obj.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% obj.errors.full_contents.each do |content| %>
        <li><%= content %></li>
      <% end %>
    </ul>
  </div>
<% end %>